# HANDOFF - post-v1 follow-up tranche (orchestrator)

Updated: 2026-07-11 (orchestrator session)
Orchestrator branch: claude/grepify-post-v1-tranche-w3i62x (off main @ da79521)
Plan source of truth: GitHub issues #39, #37, #38 (in that order). CLAUDE.md +
docs/prev1-hardening.md are binding context. The pre-v1 tranche (T1-T8 + O1) is
DONE and merged; v1.0.0 is taggable.

## Orchestration model (proven)
- One ticket = one branch off LATEST main = one PR = merged one at a time. No stacking.
  Max 2 claimed-and-unmerged PRs at once. Kyle merges manually (repo auto-merge OFF).
- Each ticket dispatched to a background sub-agent in an isolated git worktree.
- Main working dir stays on THIS orchestrator branch, never on a ticket branch.

## Hard rules (enforced)
- NO em/en dashes (U+2014/U+2013) anywhere. Use " - ".
- NO AI-authorship attribution (commits, PR bodies, author identity).
- FOOTER GOTCHA: create_pull_request auto-appends a "_Generated by [Claude Code]_"
  footer -> immediately update_pull_request with the clean body to strip it, re-fetch.
- Git identity: Datarogie <42312814+Datarogie@users.noreply.github.com> (set, verified).
- Every PR uses .github/pull_request_template.md with a PHONE-FIRST "How to test".
- make check green (ruff + mypy --strict + pytest). Never push to main.
- Token here can push but CANNOT delete remote branches (403) - leave cleanup to Kyle.

## Tranche status
  #39 health-page error capture + triage systematize  [MERGED #42]
  #37 digest index order newest->oldest by period      [MERGED #43]
  #38 "your digest" personalized view (design-led)      [PR #44 OPEN, CI green, clean - awaiting Kyle merge]

TRANCHE COMPLETE (all three delivered): #39 merged (#42), #37 merged (#43), #38 PR #44 open.

## Next tranche queued: issue #45 (Kyle, 2026-07-11)
Feed sweep - work source-FETCH errors by error class (one class per PR): try a
fetch-layer UA/Accept-header fix for http_4xx-403 + unparseable (may recover a
batch), Accept-header for the 415 flappers, TLS setting for the sslv3 ones, else
disable-with-evidence. Reddit stays quiet. HARD CONSTRAINT: build/CI cannot reach
feed hosts, so fixes verify only via the next scheduled pipeline doctor summary.
Scope is FETCH only; extract/digest/build error triage is a deferred later issue.
Start after #44 merges. Label: ready-for-agent.

## Orchestration gotcha learned this session
The Agent worktree-cleanup can transiently leave the MAIN dir checked out on a
finished sub-agent's ticket branch with a spurious dirty tree (D/M files). If you
see `git status` on a ticket branch here: DO NOT commit. The sub-agent's work is
already pushed to origin (verify local branch sha == origin sha), then recover with
`git checkout -f claude/grepify-post-v1-tranche-w3i62x && git worktree prune`.

## #38 design decisions (Kyle, 2026-07-11 - see scratchpad/your-digest-design.md)
- Persistence: localStorage for now; future direction is profiles that save
  settings + manage notifications (do NOT build that now - keep the follow-set
  store behind one accessor so a future profile layer can supersede it).
- IA: dedicated "Your digest" page (digest/yours/, server-rendered all-topics
  newest-first, JS-filtered to follows, graceful degradation + Share link) AND
  add the topic filter to the main Digests index too. Reuse index row markup +
  digests.js. Nav link in render.py. Snapshot-test the server-rendered baseline.
- Ordering stays consistent with #37 (newest-first by period). Build on merged #37.

Decision RESOLVED (Kyle, 2026-07-11): keep the ingest config as two orthogonal
fields (min_interval_hours dict + quiet_kinds list). No consolidation, no change,
no PRD diff. Closed.

## #39 investigation (DONE by orchestrator - handed to sub-agent)
Ran `grepify doctor` against origin/data (worktree). Findings:
- 128 sources; all 10 persistently-dead NON-REDDIT feeds (streak 16, http_4xx/tls/
  unparseable) are ALREADY disabled with T5 inline evidence notes. Nothing to add.
- The only ENABLED non-Reddit errors are 3 INTERMITTENT 415 flappers:
  artificial-lawyer, bdan-ai, la-biblia-de-la-ia. 4-day fetch_log shows they succeed
  on MOST fetches and 415 only intermittently (WAF/rate hiccup) -> streak 1, correctly
  unflagged. They must STAY ENABLED; disabling would drop working sources.
- ai-time-journal: fetches OK but returns 0 items every run (status "empty", 4 days).
  Not an error; a known watch item, not in #39's error-streak scope.
- Reddit (~26 sources, streak ~17): quiet by design (T6). DO NOT touch.
- Network: this env blocks all feed hosts (proxy 403) - no live feed verification
  possible, same constraint T5 hit. Work from fetch_log evidence only.

The real #39 deliverable is the issue TITLE - systematize: (a) surface error_class +
last_error on the health PAGE (model already carries them; template omits them),
(b) a repeatable/scheduled triage (add read-only `make doctor` -> $GITHUB_STEP_SUMMARY
step to pipeline.yml), (c) a triage runbook doc recording the case-by-case resolution.

Key files: grepify/site/templates/health.html (add error column), golden
tests/fixtures/site/pages/health_index.html + fixture in tests/test_site_build.py,
.github/workflows/pipeline.yml, docs/feed-triage.md (new). SourceHealth already has
error_class + last_error (grepify/health.py).

## Next concrete step
Sub-agent building #39 in a worktree. On its PR: strip footer, sweep dashes/attribution,
verify make check green, ping Kyle. Then start #37.

## Gotchas
- data branch holds truth at REPO ROOT (logs/, items/, digests/, runs/), not under data/.
  Inspect: `git worktree add --detach <path> origin/data` then --data-root <path>.
- Dash sweep: `git grep -P \x{2014}` may fail on this PCRE build; use a python filter.
- validate workflow checks report as GitHub *check runs*; pull_request_read get_status
  shows total_count 0 - use actions_list on validate.yml for real status.
